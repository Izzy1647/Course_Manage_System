const Student = require('./student_mongo')   // 操作Student数据库
const Course = require('./course_mongo')  // 操作Course数据库 课程

// Course.find(function (err, kittens) {
//     if (err) return console.error(err);
//     console.log(kittens);
// })
// // console.log('d')
